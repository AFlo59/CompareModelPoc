events {
    worker_connections 1024;
}

http {
    upstream streamlit {
        server app:8501;
    }

    # Configuration HTTP (développement)
    server {
        listen 80;
        server_name localhost;

        # Logs
        access_log /var/log/nginx/access.log;
        error_log /var/log/nginx/error.log;

        # Headers pour Streamlit
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Configuration WebSocket pour Streamlit
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        location / {
            proxy_pass http://streamlit;
            proxy_read_timeout 300s;
            proxy_connect_timeout 75s;

            # Gestion des timeouts pour les longues requêtes IA
            proxy_send_timeout 300s;
        }

        # Health check
        location /health {
            proxy_pass http://streamlit;
        }
    }

    # Configuration HTTPS (production)
    # Décommentez et configurez pour la production avec SSL
    # server {
    #     listen 443 ssl http2;
    #     server_name your-domain.com;
    #
    #     ssl_certificate /etc/nginx/ssl/cert.pem;
    #     ssl_certificate_key /etc/nginx/ssl/key.pem;
    #
    #     # Configuration SSL moderne
    #     ssl_protocols TLSv1.2 TLSv1.3;
    #     ssl_ciphers ECDHE-RSA-AES256-GCM-SHA512:DHE-RSA-AES256-GCM-SHA512;
    #     ssl_prefer_server_ciphers off;
    #
    #     # Headers de sécurité
    #     add_header X-Frame-Options DENY;
    #     add_header X-Content-Type-Options nosniff;
    #     add_header X-XSS-Protection "1; mode=block";
    #     add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload";
    #
    #     location / {
    #         proxy_pass http://streamlit;
    #         # ... autres configurations proxy
    #     }
    # }

    # Redirection HTTP vers HTTPS (production)
    # server {
    #     listen 80;
    #     server_name your-domain.com;
    #     return 301 https://$server_name$request_uri;
    # }
}
